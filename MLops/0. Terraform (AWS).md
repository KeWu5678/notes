
### IAM
role

## IaaS
### EC2
### S3
S3_handler of saving and loading the model

### DynamoDB 
### ALB (Application Load Balancer)


## PaaS
### AWS Lambda 
pay per execution, not as EC2. During low traffic, don't pay anything. 
serverless, autoscaling.
run function (code)
gain flexibility at the expense of control 

- code download
- start execution envirorment
- execute the Init code
- execute the handler code

### step function
state machine:


 


## Governance: 
### IAM policy
four required keys in any policy statement (Effect, Action, Resource, Principal)

In the context of AWS:
{
"Sid": "AllowS3Access",          ← label (optional)
"Effect": "Allow",               ← Allow or Deny                                                                         
"Action": ["s3:GetObject", ...], ← which API calls                                                                       
"Resource": ["arn:aws:s3:..."]   ← on which resources                                                                    
}                                                                                                                          
    


#### general steps
terraform init: from the github shared moduel.
terraform plan: compare the config file and the terraform state.
terraform apply: execute in the cloud to apply the differences. 
terraform destroy

#### containerization


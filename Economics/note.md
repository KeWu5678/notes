industrial upgrading->  price increasing ->     inflation ->    inetert raise ->    weakened yen ->     inflation(yen) -> hurt carry trade 
                    ->  competition(high-value)

VIX panic index